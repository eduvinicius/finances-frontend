export enum SidebarVariantEnum {
  SIDEBAR = "sidebar",
  FLOATING = "floating",
  INSET = "inset",
}

export enum SidebarCollapsibleEnum {
  OFFCANVAS = "offcanvas",
  ICON = "icon",
  NONE = "none",
}

export enum SidebarSideEnum {
  LEFT = "left",
  RIGHT = "right",
}
